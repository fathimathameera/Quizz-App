<div class="d-flex">
    <div class="mr-auto">  
        <a href="{{ route('quiz') }}" class="btn btn-primary" id="home">Home</a>
    </div>
    <div class="ml-auto">  
        <a href="{{ route('logout') }}" class="btn btn-danger">Logout</a>
    </div>
</div>